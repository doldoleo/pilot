# org.springframework.cloud:spring-cloud-starter-config 가 포함될 경우
# 아래와 같이 설정파일을 정의해 주어야 합니다.

spring.cloud.config.enable  
  - "true" 이면 지정된 config 서버로 설정 파일을 읽어와서 서버가 기동
  - "false" 이면 내 local에 있는 설정 파일을 읽어와서 서버가 기동
  - 디폴트값은 true 

spring.cloud.uri
  - config server 지정
  - 위에 값이 "true"로 지정이 되면 uri 설정을 해주어야 서버가 구동이 됩니다.
  
     